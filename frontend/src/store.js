import { writable } from "svelte/store";

export const st_team = writable();
export const st_standing = writable();

export const leagues_store = writable([]);

export const st_standings = writable([]);

export const st_teams = writable([]);

export const st_fixtures = writable([]);